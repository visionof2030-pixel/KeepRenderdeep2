name: Keep Render Server Alive

on:
  schedule:
    - cron: "*/1 * * * *" #  كل دقيقة
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: |
          curl -I https://tarafbackend.onrender.com