name: Keep Render App Alive

on:
  schedule:
    - cron: "*/1 * * * *"   # كل دقيقة
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://deep-qphc.onrender.com/ > /dev/null
